apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: be-typescript-test-apps
  description: Applications managed within the be-typescript-test repository.
spec:
  targets:
    - ./catalog/*.yaml
    - ./catalog/**/*.yaml
